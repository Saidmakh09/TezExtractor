# Real documents with real phone numbers and contacts: never commit
samples/

# Standard
node_modules/
.next/
.env
.env.local
.vercel
.DS_Store

# Internal notes, not for the public repo
CLAUDE.md

# Local test fixtures copied from samples/: never commit
public/samples/
